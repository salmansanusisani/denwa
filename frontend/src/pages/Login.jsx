// TODO(frontend): minimal/hardcoded demo login is fine per the DoD — don't over-build this.
export default function Login() {
  return (
    <div>
      <h1>Denwa — Login</h1>
      {/* TODO(frontend): a single demo-account form is enough. */}
    </div>
  );
}
